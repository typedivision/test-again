#!/bin/sh

setup () {
  echo "in setup"
}

teardown () {
  echo "in teardown"
  false
}

test_return_fail_in_teardown () {
  echo "in test"
}
#-> not ok test_return_fail_in_teardown

. test/helper
. ts
